version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    post_create_environment:
      - make install-full
    pre_build:
      - python docs/dev/prepare_notebooks.py

sphinx:
  configuration: docs/conf.py